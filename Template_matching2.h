#include <array>
#include "pixels1.h"
#include <vector>

void Template_matching2(std::vector <std::vector <int> > belongs_after,int height,int width,std::vector <std::vector <pixels1> > pixels2,std::string temp1,int frame_counter);